extends State

func enter(_msg := {}) -> void:
	print("enter walk")
#
#func update(_delta:float) -> void:
	#var target_pos = (main_char.position - owner.position).normalized()
	#if owner.position.distance_to(main_char.position) > owner.escape_distance:
		#owner.move_torwards(target_pos*owner.SPEED)

	
