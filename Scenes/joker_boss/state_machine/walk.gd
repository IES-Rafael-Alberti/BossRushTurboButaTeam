extends State

func enter(_msg := {}) -> void:
	print("enter walk")
#
#func update(_delta:float) -> void:
	#var target_pos = (owner.player.position - owner.position).normalized()
	#if owner.position.distance_to(owner.player.position) > owner.escape_distance:
		#owner.move_torwards(target_pos*owner.SPEED)
	
