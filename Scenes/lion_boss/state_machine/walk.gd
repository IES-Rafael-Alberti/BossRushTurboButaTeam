extends State

var walk_speed = 100.0

func enter(_msg := {}) -> void:
	print("walk on")

func update(_delta:float) -> void:
	if owner.position.distance_to(player_position)
	
	var direction = (owner.position)
